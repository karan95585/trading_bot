# render.yaml
services:
  - type: web
    name: intraday-trading-bot
    env: python
    # If your repo's root has Python code and requirements.txt:
    plan: starter
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app.py"
    # leave `port` blank so it defaults to $PORT
