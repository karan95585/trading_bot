services:
  - type: web
    name: intraday-trading-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    mounts:
      - type: volume
        name: trading-data
        mountPath: /data

volumes:
  - name: trading-data
    sizeGB: 1
